# Stalin Returns 测试版开发约定

## 工作范围

- 唯一默认可写的模组目录是本目录：
  `D:\Steam\steamapps\workshop\content\394360\kr_stalin_adapted_test`
- `D:\Steam\steamapps\workshop\content\394360\kr_stalin_adapted_test_local_loader`
  只是由目录联接组成的启动器投影。修改源目录，不要在加载器目录里重复修改内容。
- 所有纯数字工坊目录默认只读。尤其不要直接修改：
  - `1521695605`：Kaiserreich
  - `3391120577`：KR 共产主义拓展前置
  - `3131170989`：Kaiserreich：LKMT 内容拓展
  - `3746983015`：已发布测试版快照，也是本地开发副本的恢复来源
  - `3723313895`：另一份 Stalin Returns 订阅内容，不再作为当前测试版开发目录
- 兼容补丁应在独立兼容补丁目录中修改；不要把兼容逻辑直接写进依赖模组本体。

## 开始修改前

- 先阅读 `MOD制作日志.md`，并以最新记录和当前文件为准。历史方案经过多轮修改，不能只根据较早的对话或截图恢复旧设计。
- 运行 `git status --short --branch`。当前副本从工坊快照恢复时已带有合法的未提交改动；不得使用
  `git reset --hard`、`git checkout --` 或其他命令清除它们。
- 保留用户已有修改，避免把无关文件混入同一次提交。
- `descriptor.mod` 含发布与启动器元数据。除非任务明确涉及版本、依赖或发布信息，否则不要修改或提交它。

## 实现原则

- 只修改测试版，除非用户明确要求同步正式适配版或独立兼容补丁。
- 新存档是默认测试基线。不要为了旧存档额外增加兼容逻辑，除非用户明确要求；已有的存档修复和一次性补发逻辑不得擅自删除。
- 军事改革内容以布尔什维克和集体建设的正面叙事为核心。伏龙芝可以主导总体方向，但不能把改革写成个人英雄主义。
- 国策、事件、决议和民族精神的中文文本应采用 Kaiserreich 风格的叙事语言。效果栏显示机制和数值，描述正文不要解释后台脚本。
- 不允许让代码 ID、未本地化键、重复冒号或原始脚本片段出现在游戏界面。
- 简体中文本地化文件保持 UTF-8 BOM；修改后检查重复键和编码。
- 美术资源应避免重复图标和明显不匹配的国家、军种或时代符号。主菜单背景基线为
  `1920x1440`、4:3、无 UI 和可读文字；应按原生比例构图或裁切，不得将16:9图像纵向拉伸。

## RHoiScribe 与验证

- RHoiScribe 安装在：
  `C:\Users\ustc\.codex\mcp\RHoiScribe\rhoiscribe-windows-x86_64.exe`
- MCP 名称为 `rhoiscribe`。优先用它做环境发现、索引、引用追踪、单文件检查、项目检查和错误日志分类。
- Kaiserreich 大型覆盖项目会产生大量 CWT 基线误报。不要按红灯数量自动修复，也不要未经人工判断调用写入式修复。
- `brace_balance` 和 `unclosed_block` 红项是交付阻断项。
- 文件修改后至少执行：
  - RHoiScribe 相关文件验证；
  - `repair_hoi4_project` 的 `dry_run=true`；
  - `git diff --check`；
  - 括号、未闭合块、本地化重复键和编码检查。
- 涉及运行时触发、事件链、MIO、人物解锁、音乐或界面时，静态检查之后仍需重启游戏并用新存档实测；再依据最新 `error.log` 排查。

## 跨电脑同步

- GitHub 私有仓库是两台电脑之间唯一的开发同步源：
  `https://github.com/zxxzvxv85-dot/kr-stalin-adapted-test.git`
- 默认分支为 `main`。同一时间只允许一台电脑上的一个 Codex 修改该分支；Git 不是实时文件同步工具。换电脑前，必须让上一台电脑完成验证、提交和推送，并明确报告远端提交哈希。
- 每次开始开发前先运行：
  - `git status --short --branch`
  - `git fetch origin`
  - 工作树干净时运行 `git pull --ff-only origin main`
  - 工作树不干净时不要直接拉取、重置或覆盖；先识别改动归属，必要时向用户确认。
- 新电脑首次使用时，在 GitHub 身份验证完成后克隆私有仓库。开发副本必须放在非纯数字的可写目录中；不得克隆或覆盖 Steam 创意工坊的数字订阅目录。
- 每次结束开发前：
  - 更新 `MOD制作日志.md`；
  - 完成与改动风险相称的静态检查和游戏内测试；
  - 只暂存本次有意修改的文件并执行 `git diff --cached --check`；
  - 创建说明清晰的提交；
  - 再次 `git fetch origin`。如果远端已经前进，先在干净工作树中将本地提交变基到 `origin/main`，遇到冲突立即停止并报告，不得猜测解决；
  - `git push origin main`，并确认 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/main` 的哈希一致。
- 禁止 `git push --force`、`git reset --hard`、删除他方未合并提交，或把 Steam 创意工坊当作源码同步源。
- 如果本次工作不能安全提交并推送，必须明确告诉用户“尚未同步”，不能让另一台电脑开始开发。

## Git 与交付

- 每个提交只包含一个可说明、已验证的改动主题。
- 提交前再次检查 `git diff --check` 和暂存文件清单。
- 不要提交工坊缓存、临时输出、拒绝补丁、扫描状态或生成器中间文件。
- 最终回复应说明修改目录、验证结果、是否提交，以及仍需游戏内实测的项目。
