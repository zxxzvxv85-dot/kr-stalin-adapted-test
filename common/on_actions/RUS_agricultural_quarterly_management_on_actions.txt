on_actions = {
	on_daily = {
		effect = {
			if = {
				limit = {
					original_tag = RUS
					has_country_flag = RUS_agri_management_unlocked
				}
				if = {
					limit = { has_country_flag = RUS_nat_enabled }
					RUS_nat_daily = yes
				}
				else = { RUS_agri_daily_update = yes }
			}
		}
	}
}
