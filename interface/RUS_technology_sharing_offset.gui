# Restore the engine-required position omitted by the installed technology extension GUI.
# Identical to the current vanilla position; no technology window replacement.
guiTypes = {
 positionType = {
  name = "technology_sharing_offset"
  position = { x = 100 y = 0 }
 }
}
